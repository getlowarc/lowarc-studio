# Changelog

All notable changes to the Node Graph Runtime module are documented here. Format loosely follows
[Keep a Changelog](https://keepachangelog.com/).

## 0.1.0 - 2026-09-03

Initial release.

### Added

- `.lan` graph interpretation: start-node selection (matching the editor's own "S" badge rule),
  per-frame active-node/choices publishing, single-edge advancement via an optional `input` role.
