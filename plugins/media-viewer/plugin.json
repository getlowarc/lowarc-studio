{
  "name": "Media Viewer",
  "version": "0.2.1",
  "description": "Views images, video, and audio in formats the browser already renders natively — no bundled decoder, unlike Monaco/xterm. Exotic formats (RAW, PSD, HEIC, TIFF) aren't supported yet.",
  "contributes": {
    "viewers": [
      {
        "id": "main",
        "title": "Media",
        "entry": "index.html",
        "binary": true,
        "extensions": [
          ".png", ".jpg", ".jpeg", ".gif", ".webp", ".bmp", ".ico", ".svg", ".avif",
          ".mp4", ".webm", ".ogv",
          ".mp3", ".wav", ".flac", ".m4a", ".aac", ".opus", ".ogg"
        ]
      }
    ]
  }
}
