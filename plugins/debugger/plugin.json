{
  "name": "Debugger",
  "version": "0.1.0",
  "description": "Pause, step, and set breakpoints on a running project. Works against any module — it operates on the wire protocol every module already speaks (frame requests/replies, log lines), never anything module-internal like a source line.",
  "contributes": {
    "consoleTabs": [
      { "id": "main", "title": "Debug", "entry": "index.html" }
    ]
  }
}
